
import 'package:ledger_flutter_plus/ledger_flutter_plus.dart';

void main() {
  print('Checking ledger_flutter_plus exports');
  // Intentionally use dynamic to avoid compilation error stopping the script immediately if I can catch it? 
  // No, that won't work.
  // I will just print "Import successful" to confirm compilation.
  // And try to instantiate Ledger if it exists.
}
